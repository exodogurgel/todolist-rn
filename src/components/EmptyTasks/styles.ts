import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 48,
    alignItems: "center",
    justifyContent: "center",
  },
  title: {
    fontSize: 14,
    color: "#808080",
    fontWeight: "bold",
    marginTop: 16,
    marginBottom: 8,
  },
  subtitle: {
    fontSize: 14,
    color: "#808080",
  }
})